import React from "react";
import { Helmet } from "react-helmet";

export default class Helmet extends Component {
  render() {
    return (
      <div>
        <Helmet></Helmet>;
      </div>
    );
  }
}
